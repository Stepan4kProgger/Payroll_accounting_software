#pragma once
#include <iostream>
using std::string;

bool compare(string, string, bool);
bool compare(double, double, bool);
bool isNameLetter(char);
bool isNumericLetter(char);
bool isLoginLetter(char);
bool isListEmpty(int);